'use client'
import React from 'react'

export default function AddToCartButton() {
    return (
        <button
            onClick={() => { }}
            className="inline-flex items-center gap-2 px-4 py-2 rounded-md bg-indigo-600 hover:bg-indigo-700 text-white shadow"
        >
            Add to cart
        </button>
    )
}
